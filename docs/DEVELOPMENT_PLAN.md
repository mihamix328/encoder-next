# План развития encoder-next

Стабильная демонстрационная версия: encoder, тег v0.1.0.
Разработка ведётся в encoder-next. Каждый этап завершается сборкой и проверкой.

## Ближайшие шаги — актуально на 22 сентября 2026

- [x] Валидация UTF-8 SSID и генерация конфигураций в очищаемой памяти;
  проверка YAML отдельным парсером, строгий шаблон WPA2/CCMP.
- [x] Подготовлены автоматические Linux-проверки GitHub Actions: сборка,
  файловое восстановление, имитаторы Wi-Fi, YAML и локальный TLS.
  Linux x86_64: сборка, 10 CTest, YAML и локальный TLS прошли на cabd96d.
- [x] Подтверждён файловый слой Netplan на Linux x86_64 (не аппаратная проверка).
- [x] Досрочная отмена через журнал с защитой от чужой/подтверждённой операции;
  Linux CI прошёл. Изолированная генерация настоящим Netplan также прошла.
- [x] Проверка готовности целевой сети: SSID, WPA2/CCMP и IPv4 wlan0;
  8 Windows и 12 Linux CTest прошли. Свежий Linux-пробник не установлен на плату.
- [x] Подготовлен read-only preflight конфликтов Netplan с ограниченным YAML-парсером;
  9 Python-проверок прошли. Он не мигрирует настройки и не разрешает их применение.
- [x] Строгое чтение собственного формата и проверка структуры при записи/откате;
  локальный round trip и Linux CI прошли (a13f851).
- [x] Подготовлена связка подтверждения с сохранённым профилем и живой проверкой;
  после проверки повторно читаются файл и часы. Linux CI прошёл (ce482f3).
- [x] Добавлен экспериментальный единый обработчик подготовки, применения,
  подтверждения и отмены с журналом. Системные действия заданы интерфейсом;
  реального исполнителя и независимой службы отката пока нет.
- [x] Проверено восстановление другим процессом после принудительного завершения
  владельца транзакции; тест работает только с временными файлами и имитатором.
- [x] Реализован Linux-цикл независимого наблюдателя: повтор попыток отката,
  один экземпляр на журнал и проверка живого процесса через частный локальный
  сокет с привязкой к каталогам. Пока библиотека для будущей службы, не установка.
- [ ] Проверка конфликтующих определений wlan0 и совместимости Netplan;
  устойчивое применение WPA2-only при перезагрузке. YAML отдельно недостаточен.
- [ ] Привилегированный адаптер, независимый watchdog и их интеграция с журналом.
- [ ] API подключения/подтверждения/отмены, затем диалог в админ-клиенте.
- [ ] Согласованная аппаратная проверка и финальная установка/релиз.

Пока нет рабочего переключения Wi-Fi. Ни один черновик автоматически не
устанавливается. Старая версия encoder и сеть платы не изменяются.

Проверенная контрольная точка: 75bf8ac, GitHub Actions 35762715920 — success.
8 CTest на Windows; 13 CTest и 9 Python preflight-тестов на Linux x86_64;
отдельно прошли YAML round trip, изолированный настоящий Netplan и TLS-интеграция.
Рабочая папка: C:\Users\PC\Documents\GitHub\encoder-next, не соседний encoder.
Новый переносимый релиз не собирался; текущая работа касается сетевого backend.
Для продолжения реализовать доверенный системный исполнитель WifiManagedPlatform
и независимый watchdog для подготовленного обработчика. Не запускать netplan
apply по одному YAML: устойчивое WPA2-only и миграция старого профиля ещё не решены.

## Этап 1 — оформление

- [x] Общая тёмная тема клиента и администратора в духе GitHub.
- [x] Ограничение списка алгоритмов десятью строками с прокруткой.
- [ ] Визуальная проверка входа, основного окна, администратора и диалогов.
- [ ] Проверка масштаба Windows 125–150% и клавиатурной навигации.
- [x] Поиск алгоритмов по подстроке и компактная компоновка в два ряда.
- [x] Подписи над рамками и название окна encoder.
- [x] Пять пропусков смены пароля, затем обязательная смена при обычном закрытии.
- [ ] Проверка полного цикла смены пароля с сервером; счётчик пока локальный.

## Этап 2 — запуск со второго ПК

- [ ] Самодостаточные Windows-архивы: Qt, OpenSSL, конфигурация.
- [x] Пробная упаковка обоих клиентов с Qt, OpenSSL, Visual C++ runtime и чистыми настройками.
- [x] Проверка состава ZIP; краткий запуск отдельных процессов без авторизации на текущем ПК.
- [x] Админ-панель находит конфигурацию, сертификаты и список устройств рядом с EXE.
- [ ] Выбор адреса платы, проверка связи, сохранение подключения.
- [x] Ввод адреса/порта, выбор файла сертификата, проверка полей перед входом.
- [x] Сохранение адреса, порта и пути сертификата в профиле пользователя после входа (без пароля).
- [x] Асинхронная проверка TCP-порта через существующий сетевой код, без Qt Network.
- [ ] Доверие к сертификату и понятные ошибки TLS.
- [ ] Проверка запуска на втором ПК без среды разработки.

## Этап 3 — права и корректность операций

- [x] Переименование исходников, целей сборки и программ в encoder.
- [ ] Проверить переименованный сервер при новой установке на Orange Pi.
  Пути ГОСТ в config/server.conf — шаблоны новой установки, не готовые бинарники.
  Службу systemd при переустановке нужно настроить на encoder-server;
  текущая служба на плате этим коммитом не меняется.
- [x] Удалены неиспользуемая форма смены пароля и старая запись конфигурации; черновик соглашения перенесён в docs/archive.

Совместимость: сигнатура старых зашифрованных файлов сохранена.
Идентификатор хранилища настроек Windows сохранён, чтобы не терять
адрес подключения и счётчик пропусков смены пароля.
Пустой LICENSE.txt (содержал только разделитель) удалён: лицензия проекта
ещё не выбрана. Лицензии сторонних библиотек этим не изменяются.

- [ ] Проверить реальное применение режима ГОСТ: сейчас подсказка говорит,
      что настройка интерфейсная и реализация определяется бинарником.
- [ ] Проверить поддержку алгоритмов и допустимые сочетания параметров.
- [ ] Серверная проверка ролей и ограничений.
- [x] Разрешения на шифрование/расшифрование: настройка в админ-панели, серверная проверка, все четыре комбинации проверены локальным TLS-тестом.
- [ ] Ограничения отдельных алгоритмов и именованные роли (админ-доступ сейчас через токен).
- [x] Удалён дублирующий неполный обработчик админ-порта: порты используют единую реализацию с проверкой токена.
- [x] Локальный TLS-тест: административные команды на обоих портах, отказ неверному токену, смена пароля и отказ старому паролю.
- [x] Создание, постоянная блокировка пользователей, сброс пароля через администратора (локальные TLS-тесты пройдены).
- [ ] Установка и проверка управления пользователями на Orange Pi.
- [ ] Управление привязкой устройств, понятные причины отказов.

## Этап 4 — сеть платы

- [x] Подготовлен статус текущего Wi-Fi через фильтрованный STATUS и снимок v2;
  Windows TLS/Qt-тесты прошли, новый сервер сохраняет чтение списка из v1.
- [x] ARM64: v2-сборщик собран, четыре CTest и TLS-интеграция v1/v2 прошли.
- [x] Реальные SCAN_RESULTS и STATUS проверены во временной ограниченной службе systemd.

- [x] Локально добавлены фильтр SSID/BSSID, числовая сортировка Wi-Fi,
  сохранение предыдущего результата при ошибке обновления и соответствующие Qt-тесты.

- [x] Пользователь подтвердил чтение реального Wi-Fi-кэша через --check (119 байт).
- [x] На ARM64 прошёл изолированный тест публикации снимка: атомарная замена,
  права 0640, защита от небезопасного каталога и проверки символьных ссылок.
- [x] Одноразовая публикация через systemd и чтение производственным парсером
  под encoder-diag прошли 16 сентября; запись читателю запрещена.
- [ ] Постоянная установка службы/таймера с рабочей группой encoder при финальном переносе.

- [x] На Orange Pi ARM64 прошли изолированные wifi-socket и network-snapshot;
  сборщик скомпилирован с -Werror. Всё в отдельном каталоге encoder-diag,
  рабочие службы не менялись. Проверка реального сокета и systemd ещё впереди.

- [x] Доступ к управляющему Wi-Fi-сокету вынесен из сервера в отдельный
  локальный сборщик. Добавлены экспериментальные service/timer, read-only
  снимки, ограничение размера и срока давности; TLS-тесты чтения прошли.
  Проверка Linux-сборщика, прав каталогов и служб на плате ещё требуется.

- [x] Подготовлено чтение кэша Wi-Fi через SCAN_RESULTS и кнопка в админ-панели.
  Функция выключена по умолчанию; изолированный Linux-тест уже прошёл.
  Ограничения и план разграничения прав описаны в docs/NETWORK.md.

- [x] Подтверждена конфигурация платы: Netplan + systemd-networkd,
  netplan-wpa-wlan0, управляющий интерфейс wpa_cli отвечает PONG.
- [x] Реализован read-only API admin_network_status и окно «Сеть платы»
  в админ-клиенте: интерфейсы с IPv4 и состояние carrier, только с токеном.
  Сборка и отказ доступа проверены на Windows; чтение интерфейсов проверено на ARM64.
  Работа окна с новой службой платы ещё не проверена. Без IPv4 интерфейсы пока не выводятся.

- [ ] Состояние подключения, IP, список Wi-Fi и уровень сигнала.
- [ ] Подключение к домашней Wi-Fi по SSID и паролю с правами администратора.
- [ ] Защищённая передача пароля без журналирования.
- [ ] Откат к прежней сети при неудаче и повторное обнаружение платы.
- [ ] Ethernet через роутер и прямое соединение с ПК.
- [ ] Определить адресацию прямого Ethernet и способ первого подключения.
- [ ] Отдельно оценить Wi-Fi с логином и режим точки доступа платы.

Текущая разработка подключения Wi-Fi без аппаратных проверок:

- [x] Валидация WPA2-профиля и хранение производного PSK в очищаемой памяти.
- [x] Ядро подтверждения/таймаута/отката и тесты с Fake-backend.
- [x] Linux-журнал: атомарная запись/fsync, закрытые права, блокировка, SHA-256,
  срок по boot time и обнаружение незавершённой операции после перезагрузки.
- [x] Шаг независимого восстановления и файловые тесты после SIGKILL/по сроку.
- [ ] Подключить журнал к production watchdog и реальному адаптеру Netplan.
  Добавлены файловый слой собственного 90-encoder-wifi.yaml и связка восстановления
  с подтверждением успешного применения. Linux-тест netplan-files подготовлен,
  но ещё не выполнен: SSH недоступен, локального WSL нет. Реальные команды отключены.
  Контрольная точка: 8 CTest на ARM64 и 6 на Windows пройдены;
  реальное переключение Wi-Fi не выполнялось. Журнал и наблюдатель —
  отдельная библиотека encoder_wifi_recovery, ещё не подключённая к рабочей службе.
- [ ] Настоящий адаптер Netplan, API и GUI подключения.
Подробности и границы готовности: docs/WIFI_CONNECTION.md. Плату не переключать.

## Этап 5 — выпуск

- [ ] Шифрование/расшифрование и совместимость файлов v0.1.0.
- [ ] Обрыв связи, восстановление, ограничения обычного пользователя.
- [ ] Обновить README и orange_pi_commands.txt.
- [ ] Подготовить новый релиз с обоими клиентами.

## Актуальные сборки и проверки — 12 сентября 2026

Windows EXE: build/windows-client/client/Release/encoder-client.exe
и build/windows-client/admin/Release/encoder-admin.exe.
Переносимые архивы: build/portable/encoder-network-20260913/.
Это пробные комплекты, не финальный релиз; сертификата и токенов платы в них нет.

Пять тестов Windows CTest прошли: net-loopback, ui-1, ui-1.25, ui-1.5,
admin-network-ui. Проверяются TCP и таймаут, компактный список и поиск,
права, счётчик смены пароля, фоновый запрос сети, Wi-Fi таблица и раннее закрытие.
Запуск: ctest --test-dir build/windows-client -C Release --output-on-failure.
Для Qt задайте QT_PLUGIN_PATH на Qt6/plugins установленного vcpkg;
тесты используют платформу minimal. Автотесты не заменяют визуальную оценку.

На Orange Pi под encoder-diag отдельно собрана полная тестовая серверная версия.
Каталог: /home/encoder-diag/encoder-check-ECm1Ll/build-614e8ab.
Прошли net-loopback, wifi-socket, network-snapshot.
TLS-интеграция прошла на Windows и ARM64: пользователи, права, блокировка,
смена пароля, перезапуск, проверка токена, Wi-Fi снимки.
Проверены AES-256-GCM round trip с клиентским/серверным ключом,
пустые/бинарные данные и отказ при повреждении шифротекста.
ГОСТ, старый файловый контейнер и рабочая установка этими тестами не проверены.

## Следующая контрольная точка

SSH доступен как encoder-diag@172.10.0.2, без административных прав.
Рабочий сервер не заменять, сеть не переключать.
Режим --check прочитал реальный Wi-Fi-сокет в root-сеансе пользователя.
Одноразовый запуск через systemd и публикация в /run/encoder-network проверены:
root:encoder-diag, файл 0640, каталог 0750; encoder-diag читает, но не изменяет.
Производственный парсер прочитал свежий снимок v2. Постоянный таймер не установлен.
systemd-analyze verify нашёл отсутствующие /opt/encoder/bin/* — новая версия
намеренно не установлена. Это не подтверждение работы служб и их прав.

Для аппаратной проверки сборщика потребуется отдельно согласованный запуск
с нужными правами. Не выдавать основному серверу root или общий sudo.
Затем: новый поиск Wi-Fi, смена сети с откатом, повторное обнаружение платы,
проверка привязки ПК и обычного пользователя, финальные тесты/перенос/релиз.
Старую установку удалять только после резервной копии и проверки расшифрования.

## Поиск Wi-Fi — контрольная точка 16 сентября 2026

- [x] Фиксированная команда SCAN в отдельном сборщике, режим --scan.
- [x] Сохраняемое между запусками ограничение: одна попытка за 30 секунд.
  Файл состояния закрыт от обычного сервера; блокировка исключает одновременные заявки.
- [x] ARM64: пять CTest, включая ответы OK/FAIL-BUSY/отказ на имитаторе сокета
  и проверки повреждённого состояния, прав и символических ссылок.
- [x] Авторизованный API admin_wifi_scan и отдельный socket-activated обработчик.
  Основной сервер не получает root или доступ к сокету supplicant. По умолчанию выключено.
- [x] Кнопка поиска, ожидание события завершения, обновление результатов в админ-панели.
  При ошибке прежний список остаётся, закрытие окна безопасно и не отменяет поиск.
- [x] Аппаратная проверка socket activation: encoder-scan-check-67315d5a1e1e,
  запрос от encoder-diag, 11 строк; nobody не имеет права записи в сокет.
  Временные unit-файлы удалены. Рабочая установка не менялась.
- [x] Настоящий поиск через TLS и отдельный непривилегированный тестовый сервер:
  encoder-scan-check-8b7fbdd69678, 10 строк, отказ неверному токену и повтору
  по cooldown. Тестовый сервер, данные и runtime unit-файлы удалены.
- [x] Настоящий поиск из Windows: ручная цель encoder-admin-live-check использует
  производственные NetworkDialog и AdminClient, TLS по Ethernet к отдельному
  серверу. Получены 13 строк, повтор отклонён по cooldown, таблица сохранена.
  Сохранённые настройки клиента и рабочий сервер не менялись.
- [x] Согласованный запуск реального сканирования на плате: run-u90.service,
  11 строк результатов, код 0, 4,348 секунды. Проверены реальный обработчик
  через socketpair и ограничения systemd, не socket activation или установленный UI.

Ответ OK на SCAN означает принятие запроса, а не завершение поиска.
Новые службы не установлены, рабочий сервер и сетевые настройки не изменены.

Проверено после реализации: 6 CTest на ARM64, 5 CTest на Windows, TLS-интеграция
на обеих платформах. На ARM64 включённый API проверен с локальным имитатором
обработчика; тест не обращается к /run или реальному радио.
Свежие пробные переносимые комплекты: build/portable/encoder-scan-20260916/.
Архивы encoder-network-20260913 старее и не содержат этой кнопки.

Текущая схема: клиент → TLS/admin_token → сервер → закрытый UNIX-сокет
→ отдельный обработчик → ATTACH/SCAN/событие/SCAN_RESULTS. Данные возвращаются
напрямую, без перезаписи периодического снимка. ARM64 scan-flow использует
имитатор supplicant; это не аппаратная проверка поиска.
