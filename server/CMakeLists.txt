add_executable(encoder-server
  src/main.cpp
  src/audit.cpp
  src/monitor.cpp
  src/admin_server.cpp
  src/network_status.cpp
  src/scan_ipc.cpp
)

target_link_libraries(encoder-server PRIVATE encoder_common Threads::Threads)
target_include_directories(encoder-server PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)

if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
  add_library(encoder_wifi_probe STATIC src/wifi_target_probe.cpp src/wifi_control.cpp)
  target_include_directories(encoder_wifi_probe PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_link_libraries(encoder_wifi_probe PUBLIC encoder_common)
  add_library(encoder_wifi_recovery STATIC src/wifi_journal.cpp src/wifi_recovery_worker.cpp src/netplan_files.cpp src/wifi_managed_backend.cpp src/wifi_recovery_supervisor.cpp)
  target_include_directories(encoder_wifi_recovery PUBLIC ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_link_libraries(encoder_wifi_recovery PUBLIC encoder_common)
  add_executable(encoder-wifi-scan-helper src/scan_helper.cpp src/scan_ipc.cpp src/scan_limit.cpp src/scan_session.cpp)
  target_include_directories(encoder-wifi-scan-helper PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  add_executable(encoder-wifi-collector src/wifi_collector.cpp src/wifi_control.cpp src/snapshot_publish.cpp src/network_status.cpp src/scan_limit.cpp)
  target_include_directories(encoder-wifi-collector PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
endif()

if(BUILD_UI_TESTS AND CMAKE_SYSTEM_NAME STREQUAL "Linux")
  add_executable(encoder-wifi-supervisor-tests tests/wifi_recovery_supervisor_checks.cpp)
  target_link_libraries(encoder-wifi-supervisor-tests PRIVATE encoder_wifi_recovery Threads::Threads)
  add_test(NAME wifi-recovery-supervisor COMMAND encoder-wifi-supervisor-tests)
  set_tests_properties(wifi-recovery-supervisor PROPERTIES TIMEOUT 20)
  add_executable(encoder-wifi-managed-backend-tests tests/wifi_managed_backend_checks.cpp)
  target_link_libraries(encoder-wifi-managed-backend-tests PRIVATE encoder_wifi_recovery Threads::Threads)
  add_test(NAME wifi-managed-backend COMMAND encoder-wifi-managed-backend-tests)
  set_tests_properties(wifi-managed-backend PROPERTIES TIMEOUT 20)
  add_executable(encoder-wifi-target-probe-tests tests/wifi_target_probe_checks.cpp)
  target_link_libraries(encoder-wifi-target-probe-tests PRIVATE encoder_wifi_probe Threads::Threads)
  add_test(NAME wifi-target-probe COMMAND encoder-wifi-target-probe-tests)
  set_tests_properties(wifi-target-probe PROPERTIES TIMEOUT 15)
  add_executable(encoder-netplan-files-tests tests/netplan_files_checks.cpp)
  target_link_libraries(encoder-netplan-files-tests PRIVATE encoder_wifi_recovery)
  add_test(NAME netplan-files COMMAND encoder-netplan-files-tests)
  set_tests_properties(netplan-files PROPERTIES TIMEOUT 15)
  add_executable(encoder-wifi-journal-tests tests/wifi_journal_checks.cpp)
  target_include_directories(encoder-wifi-journal-tests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_link_libraries(encoder-wifi-journal-tests PRIVATE encoder_wifi_recovery)
  add_test(NAME wifi-recovery-journal COMMAND encoder-wifi-journal-tests)
  set_tests_properties(wifi-recovery-journal PROPERTIES TIMEOUT 15)
  add_executable(encoder-scan-flow-tests tests/scan_flow_checks.cpp src/scan_ipc.cpp src/scan_session.cpp)
  target_include_directories(encoder-scan-flow-tests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_link_libraries(encoder-scan-flow-tests PRIVATE Threads::Threads)
  add_test(NAME scan-flow COMMAND encoder-scan-flow-tests)
  set_tests_properties(scan-flow PROPERTIES TIMEOUT 30)
  add_executable(encoder-wifi-tests tests/wifi_socket_checks.cpp src/wifi_control.cpp)
  target_include_directories(encoder-wifi-tests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  target_link_libraries(encoder-wifi-tests PRIVATE Threads::Threads)
  add_test(NAME wifi-socket COMMAND encoder-wifi-tests)
  add_executable(encoder-snapshot-tests tests/network_snapshot_checks.cpp src/network_status.cpp)
  target_include_directories(encoder-snapshot-tests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  add_test(NAME network-snapshot COMMAND encoder-snapshot-tests)
  add_executable(encoder-publish-tests tests/snapshot_publish_checks.cpp src/snapshot_publish.cpp src/network_status.cpp)
  target_include_directories(encoder-publish-tests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  add_test(NAME snapshot-publish COMMAND encoder-publish-tests)
  add_executable(encoder-scan-limit-tests tests/scan_limit_checks.cpp src/scan_limit.cpp)
  target_include_directories(encoder-scan-limit-tests PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/src)
  add_test(NAME scan-cooldown COMMAND encoder-scan-limit-tests)
endif()

if(WIN32)
  target_link_libraries(encoder-server PRIVATE ws2_32)
endif()
