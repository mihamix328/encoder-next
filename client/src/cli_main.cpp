#include "client_core.h"
#include "cipheator/config.h"

#include <iostream>

int main(int argc, char** argv) {
  std::cout << "Cipheator CLI client is not implemented. Build with -DBUILD_GUI=ON for Qt client." << std::endl;
  return 0;
}
