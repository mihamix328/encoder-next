#include "client_core.h"
#include "encoder/config.h"

#include <iostream>

int main(int argc, char** argv) {
  std::cout << "Encoder CLI client is not implemented. Build with -DBUILD_GUI=ON for Qt client." << std::endl;
  return 0;
}
